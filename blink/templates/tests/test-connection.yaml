apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blink.fullname" . }}-test-connection"
  labels:
    {{- include "blink.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
